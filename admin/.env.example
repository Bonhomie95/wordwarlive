# API origin the Vite dev-server proxies /api to. Defaults to http://localhost:4000.
# Point this at your running WordWar server (dev) or leave unset in production
# where the admin is served behind the same origin / reverse proxy as the API.
VITE_API_TARGET=http://localhost:6010
